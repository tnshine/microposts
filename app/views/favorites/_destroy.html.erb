<%= link_to favorite_path(current_user.favorites.find_by(micropost_id: micropost.id)), remote: true, method: :delete, class: 'favorite-link' do %>
  <i class="fa fa-star favorite-star"></i>
<% end %>
(<%= micropost.favoriting_users.count %>)
<% micropost.favoriting_users.each do |user| %>
  <%= link_to gravatar_for(user, size: 16), user, class: "contentGravatar" %>
<% end %>
