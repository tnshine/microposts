<%= link_to favorites_path(micropost_id: micropost.id), remote: true, method: :post, class: 'favorite-link' do %>
  <i class="fa fa-star-o unfavorite-star"></i>
<% end %>
(<%= micropost.favoriting_users.count %>)
<% micropost.favoriting_users.each do |user| %>
  <%= link_to gravatar_for(user, size: 16), user, class: "contentGravatar" %>
<% end %>
